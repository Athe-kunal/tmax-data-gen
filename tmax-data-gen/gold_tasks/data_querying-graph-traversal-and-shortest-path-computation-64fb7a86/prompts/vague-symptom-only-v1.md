I'm managing backups across a network of database servers and need to figure out the most efficient route between two specific nodes based on connection weights. Right now I have the connection data sitting in a file and the start/end nodes in separate files, but I can't get a working script that reads them, computes the best path, and writes the result somewhere I can check. I just want it to run cleanly and produce the right answer so my verification step passes.
