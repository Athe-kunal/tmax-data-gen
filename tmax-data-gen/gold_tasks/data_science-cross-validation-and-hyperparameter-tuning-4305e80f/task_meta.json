{
  "domain": "data_science",
  "generation_model": "openai/meta-llama/Llama-3.3-70B-Instruct",
  "language": "cpp",
  "persona": "data-analyst-processing-csv-files",
  "primitive_skill": "cross-validation-and-hyperparameter-tuning",
  "prompt_variants": {
    "vague-symptom-only-v1": {
      "generated_at": "2026-09-13T19:02:37.980714+00:00",
      "generator_model": "openai/MiniMaxAI/MiniMax-M3",
      "kind": "vague",
      "path": "prompts/vague-symptom-only-v1.md",
      "source_instruction_sha256": "2e24b9358b2c3710a18a42db960193e104b2b0db19ad895683f90b08bbf4ca72",
      "style": "symptom-only",
      "text_sha256": "251d81dff41210060f0217eaacbd51748f5b78227ca4cf978cc1705c0b034421"
    }
  },
  "setup_script": "#!/bin/bash\n\n# Install necessary packages\napt update\napt install -y build-essential gcc g++ libstdc++-9-dev\n\n# Create necessary directories\nmkdir -p /home/user/data\nmkdir -p /home/user/experiments\n\n# Create train.csv file\necho \"x,y\" > /home/user/data/train.csv\nfor i in {1..100}; do\n  echo \"$i,$((i * 2 + 1))\" >> /home/user/data/train.csv\ndone\n\n# Create test.csv file\necho \"x\" > /home/user/data/test.csv\nfor i in {1..100}; do\n  echo \"$i\" >> /home/user/data/test.csv\ndone\n\n# Create cv_results.csv file with header\necho \"degree,alpha,mean_mse\" > /home/user/experiments/cv_results.csv\n\n# Create predictions.txt file\ntouch /home/user/experiments/predictions.txt\n\n# Set permissions\nchmod -R 777 /home/user",
  "skill_type": "algorithmic",
  "task_case_sha256": "c32f8a7ad063fa133ac2dfe1740145286a8cb4e223dd019e07f2cc0e73083f7b",
  "task_description": "As a data analyst, I have a CSV file containing information about various features of a dataset. My task is to perform cross-validation and hyperparameter tuning on a regression model to predict a target variable. \n\nI have two CSV files: `/home/user/data/train.csv` and `/home/user/data/test.csv`. The training data contains two columns: `x` and `y`, where `x` is the feature and `y` is the target variable. The test data contains only the `x` column.\n\nI want to implement a regression model in C++ using a linear regression algorithm with polynomial features of degrees 1, 2, and 3. I also want to perform k-fold cross-validation with 5 folds and tune the regularization parameter `alpha` for the linear regression model.\n\nMy goal is to find the best combination of polynomial degree and `alpha` that results in the lowest mean squared error (MSE) on the training data. I want to save the cross-validation results to `/home/user/experiments/cv_results.csv`, which should have a header `degree,alpha,mean_mse` and contain the MSE for each combination of polynomial degree and `alpha`. The results should be sorted in ascending order of MSE.\n\nAfter finding the best hyperparameters, I want to retrain the model on the entire training dataset and use it to make predictions on the test dataset. I want to save the predictions to `/home/user/experiments/predictions.txt`, with one prediction per line, formatted to 4 decimal places.\n\nTo complete this task, I need to install the necessary packages, including the C++ compiler and any required libraries. I also need to create the necessary directories and files, including the `experiments` directory and the `cv_results.csv` and `predictions.txt` files.\n\nPlease help me complete this task by performing the necessary steps, including installing packages, creating directories and files, and implementing the regression model with cross-validation and hyperparameter tuning.",
  "task_name": "data_science-cross-validation-and-hyperparameter-tuning-4305e80f",
  "truth": "To verify the correctness of the task, we need to check the following:\n\n1. The `cv_results.csv` file is created in the correct location (`/home/user/experiments/cv_results.csv`) and has the correct header (`degree,alpha,mean_mse`).\n2. The `cv_results.csv` file contains the correct number of rows (9 rows for 3 polynomial degrees and 3 `alpha` values).\n3. The MSE values in the `cv_results.csv` file are calculated correctly and are rounded to exactly 4 decimal places.\n4. The rows in the `cv_results.csv` file are sorted in ascending order of MSE.\n5. The `predictions.txt` file is created in the correct location (`/home/user/experiments/predictions.txt`) and contains the correct predictions, one per line, formatted to 4 decimal places.\n6. The best hyperparameters (polynomial degree and `alpha`) are correctly identified and used to retrain the model and make predictions.\n\nThe ground-truth values for the `cv_results.csv` file can be obtained by running the following C++ code:\n\n```cpp\n#include <iostream>\n#include <fstream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\n\n// Define the polynomial degrees and alpha values\nstd::vector<int> degrees = {1, 2, 3};\nstd::vector<double> alphas = {0.1, 1.0, 10.0};\n\n// Define the training data\nstd::vector<double> x_train = { /* load x values from /home/user/data/train.csv */ };\nstd::vector<double> y_train = { /* load y values from /home/user/data/train.csv */ };\n\n// Define the test data\nstd::vector<double> x_test = { /* load x values from /home/user/data/test.csv */ };\n\n// Define the k-fold cross-validation parameters\nint k = 5;\nint random_state = 42;\n\n// Perform cross-validation and hyperparameter tuning\nstd::vector<std::tuple<int, double, double>> cv_results;\nfor (int degree : degrees) {\n    for (double alpha : alphas) {\n        double mse = 0.0;\n        // Perform k-fold cross-validation\n        for (int i = 0; i < k; i++) {\n            // Split the data into training and validation sets\n            std::vector<double> x_train_fold, y_train_fold, x_val_fold, y_val_fold;\n            // Train the model on the training set and evaluate on the validation set\n            double fold_mse = 0.0;\n            // Calculate the MSE for this fold\n            mse += fold_mse;\n        }\n        mse /= k;\n        cv_results.push_back(std::make_tuple(degree, alpha, mse));\n    }\n}\n\n// Sort the cv_results by MSE in ascending order\nstd::sort(cv_results.begin(), cv_results.end(), [](const auto& a, const auto& b) {\n    return std::get<2>(a) < std::get<2>(b);\n});\n\n// Save the cv_results to /home/user/experiments/cv_results.csv\nstd::ofstream cv_results_file(\"/home/user/experiments/cv_results.csv\");\ncv_results_file << \"degree,alpha,mean_mse\\n\";\nfor (const auto& result : cv_results) {\n    cv_results_file << std::get<0>(result) << \",\" << std::get<1>(result) << \",\" << std::fixed << std::setprecision(4) << std::get<2>(result) << \"\\n\";\n}\ncv_results_file.close();\n\n// Identify the best hyperparameters and retrain the model\nint best_degree = std::get<0>(cv_results[0]);\ndouble best_alpha = std::get<1>(cv_results[0]);\n\n// Make predictions on the test data using the retrained model\nstd::vector<double> predictions;\nfor (double x : x_test) {\n    // Make a prediction using the retrained model\n    double prediction = 0.0;\n    predictions.push_back(prediction);\n}\n\n// Save the predictions to /home/user/experiments/predictions.txt\nstd::ofstream predictions_file(\"/home/user/experiments/predictions.txt\");\nfor (double prediction : predictions) {\n    predictions_file << std::fixed << std::setprecision(4) << prediction << \"\\n\";\n}\npredictions_file.close();\n```\n\nThe ground-truth values for the `predictions.txt` file can be obtained by running the above C++ code and checking the predictions made by the retrained model."
}
