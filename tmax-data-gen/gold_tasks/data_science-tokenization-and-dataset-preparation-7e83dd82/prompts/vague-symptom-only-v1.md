I'm chasing down a training-serving feature skew problem and the preprocessing and inference numbers I'm getting don't match what I expect from serving. I have a text corpus and vocabulary that need to flow through a tokenization step and a small neural network, with the results saved as summary statistics. I just need a reproducible pipeline that produces a clean metrics file I can diff against the serving side.
