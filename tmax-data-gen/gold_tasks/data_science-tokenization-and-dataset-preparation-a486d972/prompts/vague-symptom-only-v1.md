I've got a text corpus and a vocabulary file I need to turn into tokenized data and run through a small PyTorch model to get some summary statistics. The whole pipeline should leave me with a memory-mapped dataset file and a metrics file with token count and output stats, and I want it to be reproducible with a fixed seed. Can you put together a clean, modular script that handles the tokenization, model definition, inference, and metrics output without messing with my original input files?
