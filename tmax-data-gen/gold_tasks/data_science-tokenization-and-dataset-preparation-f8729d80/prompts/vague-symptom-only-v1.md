I have a text corpus and a vocabulary file I need to convert into integer IDs for a small embedding model I'm prototyping, but I'm not sure how to handle words that aren't in the vocab. Once that's set up, I want to run the first chunk of tokens through a simple embedding-plus-linear setup and capture the mean and spread of the output so I can sanity-check the model. I'd like the tokenized data and those summary numbers saved somewhere I can grab later.
