schema_version = "1.1"

[task]
name = "tmax/file_operations-filesystem-mount-and-path-manipulation-c350182e"
description = "As a storage administrator managing disk space, I need to automate the process of exporting data from a legacy tool and setting up a new mount structure using symbolic links. I have a legacy export to"

[metadata]
source = "tmax-data-gen knowledge graph"
domain = "file_operations"
skill_type = "navigation"
primitive_skill = "filesystem-mount-and-path-manipulation"
persona = "storage-administrator-managing-disk-space"
language = "c"
generation_model = "openai/meta-llama/Llama-3.3-70B-Instruct"

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
cpus = 1
memory_mb = 2048
allow_internet = true
