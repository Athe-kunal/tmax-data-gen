schema_version = "1.1"

[task]
name = "tmax/file_operations-multi-line-log-record-parsing-c930229d"
description = "As a configuration manager, I need to track changes to a log file and extract specific records for auditing purposes. The log file is located at `/home/user/logs/config.log` and contains multi-line lo"

[metadata]
source = "tmax-data-gen knowledge graph"
domain = "file_operations"
skill_type = "data-parsing"
primitive_skill = "multi-line-log-record-parsing"
persona = "configuration-manager-tracking-changes"
language = "go"
generation_model = "openai/meta-llama/Llama-3.3-70B-Instruct"

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
cpus = 1
memory_mb = 2048
allow_internet = true
