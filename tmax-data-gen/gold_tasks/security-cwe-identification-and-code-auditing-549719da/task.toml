schema_version = "1.1"

[task]
name = "tmax/security-cwe-identification-and-code-auditing-549719da"
description = "As a network engineer inspecting traffic, I need to analyze a custom script that verifies a JSON Web Token (JWT)-like structure and validates its cryptographic signature against a certificate chain. T"

[metadata]
source = "tmax-data-gen knowledge graph"
domain = "security"
skill_type = "testing"
primitive_skill = "cwe-identification-and-code-auditing"
persona = "network-engineer-inspecting-traffic"
language = "python"
generation_model = "openai/meta-llama/Llama-3.3-70B-Instruct"

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
cpus = 1
memory_mb = 2048
allow_internet = true
